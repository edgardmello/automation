---
- hosts: localhost
  connection: local
  become: true

  tasks:
    - name: Install Ansible
      apt:
        name:
          - htop
        state: present
        update_cache: yes
        cache_valid_time: 3600
